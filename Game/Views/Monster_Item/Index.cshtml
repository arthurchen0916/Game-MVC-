@model IEnumerable<Game.Models.Monster_Item>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MonsterID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ItemName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MonsterID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ItemName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.MonsterID}) |
            @Html.ActionLink("Details", "Details", new { id = item.MonsterID}) |
            @Html.ActionLink("Delete", "Delete", new { id = item.MonsterID})
        </td>
    </tr>
}

</table>
